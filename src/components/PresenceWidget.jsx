import { useEffect, useState } from "react";

// PresenceWidget disabled for static GitHub Pages deployment
// (requires local server at localhost:3001)
export default function PresenceWidget() {
  return null;
}